import React from 'react';
import { Link } from 'react-router-dom';

const NotFound = () => (
    <div>
        <div>Not Found</div>

        <Link to="/">Home</Link>
    </div>
);

export default NotFound;